<UserControl x:Class="CarSales.View.MainView.Catalog.CatalogUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarSales.View.MainView.Catalog"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Background="Black">
        <TextBlock Text="Создать объявление" Style="{StaticResource styleTextBlock}" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Top"/>

        <StackPanel x:Name="MarkModelCar" Margin="100">
            <TextBlock VerticalAlignment="Center" Text="Марка" Style="{StaticResource styleTextBlock}" Margin="5" HorizontalAlignment="Center"/>
            <Border Background="White" Margin="10" CornerRadius="10" Height="40" Width="250">
                <TextBox x:Name="MarkTextBox" Style="{StaticResource styleTextBox}" Margin="5" VerticalAlignment="Center"  />
            </Border>

            <TextBlock VerticalAlignment="Center" Text="Модель" Style="{StaticResource styleTextBlock}" Margin="5" HorizontalAlignment="Center"/>
            <Border Background="White" Margin="10" CornerRadius="10" Height="40" Width="250">
                <TextBox x:Name="ModelTextBox" Style="{StaticResource styleTextBox}" Margin="5" VerticalAlignment="Center"  />
            </Border>

        </StackPanel>
        
    </Grid>
</UserControl>
